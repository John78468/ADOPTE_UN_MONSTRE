<%= image_tag "logobm.png" %>


<div class="card-product">
    <% @monsters.each do |monster| %>

    <div class="border my-3 p-5 text-center">
      <%= image_tag "goblin1.png" , width:"450px"  %>
      <div class="card-product-infos">
        <%= link_to monster_path(monster) do %>
          <p class="display-5 text-decoration-none">
            <%= monster.name %>
          </p>
        <% end %>
      </div>
      </div>
    <% end %>
  </div>
